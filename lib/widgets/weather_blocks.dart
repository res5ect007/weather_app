import 'package:flutter/material.dart';

class WeatherBlocks extends StatelessWidget {
  const WeatherBlocks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
